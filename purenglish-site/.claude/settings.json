{
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Write|Edit",
        "hooks": [
          {
            "type": "command",
            "command": "echo '{\"systemMessage\": \"📝 Reminder: if this change affects site structure, pages, or dependencies — update CLAUDE.md.\"}'"
          }
        ]
      }
    ]
  }
}
